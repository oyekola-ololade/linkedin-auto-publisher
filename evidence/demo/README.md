# Demo Placeholder

**Status:** BLOCKED BY CURRENT COMPATIBILITY / DELAY-LOGIC ISSUES.

The checked-in template still uses the legacy LinkedIn `ugcPosts` pattern and its `Wait 24h` node is a Set node rather than a real delay. Do not publish a working demo until the API path/auth requirements are updated, real delayed execution is implemented and a test account run succeeds.